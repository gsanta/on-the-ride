// Generated by CoffeeScript 1.7.1
(function() {
  angular.module("services").factory("Map", function() {
    var factoryObj;
    factoryObj = {
      createMap: function(centerPosition, zoom, domElement, mapTypeId) {
        var mapProp;
        mapProp = {
          center: centerPosition,
          zoom: zoom,
          mapTypeId: google.maps.MapTypeId.ROADMAP
        };
        if (mapTypeId != null) {
          mapProp.mapTypeId = mapTypeId;
        }
        return new google.maps.Map(domElement, mapProp);
      },
      addRoute: function(route, map) {
        var coordinate, coordinates, routePolyline, _i, _len;
        coordinates = [];
        for (_i = 0, _len = route.length; _i < _len; _i++) {
          coordinate = route[_i];
          coordinates.push(new google.maps.LatLng(coordinate.lat, coordinate.lon));
        }
        routePolyline = new google.maps.Polyline({
          path: coordinates,
          geodesic: true,
          strokeColor: '#FF0000',
          strokeOpacity: 1.0,
          strokeWeight: 2
        });
        return routePolyline.setMap(map);
      },
      createCoordinate: function(lat, lon) {
        return new google.maps.LatLng(lat, lon);
      }
    };
    return factoryObj;
  });

}).call(this);
