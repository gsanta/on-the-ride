angular.module 'services'
.factory 'LocalDataProviderService', ( MapConstants ) ->
  
  factoryObj =

    loadRouteInfo: ( zoom, maps ) ->
      console.log "indexed"
      console.log indexed('myDB').create();
      1

    clearAndPopulateDbWithDummyData: () ->

  factoryObj